# 万物心理学 · Mind of Everything —— 产品需求文档（PRD）

| 字段 | 值 |
| --- | --- |
| 文档版本 | v0.1（初稿） |
| 日期 | 2026-09-21 |
| 状态 | 待评审 |
| 项目名（中文） | 万物心理学 |
| 项目名（英文） | Mind of Everything |
| 一句话定位 | 用心理学读懂人心：从认知偏差、著名实验到流派与思想家，把「心智」拆成看得懂的常识。 |
| 仓库 slug（建议） | `mind-everything` |
| 姊妹项目 | 万物经济学（`econ-everything`）、中华王朝 · 千年脉络（`chinese-dynasty-timeline`） |

---

## 1. 背景与目标

### 1.1 背景
- 已有成功先例：姊妹项目「万物经济学」以**零框架、Vite 打包、中英双语、数据驱动**的方式做经济学通识，架构成熟、体验良好、可复用；更早的「中华王朝 · 千年脉络」以纯静态方式做历史科普，验证了「模块导航 + 卡片/时间线 + 明暗主题 + 双语」的交互范式。
- 心理学是高频刚需：大众对拖延、情绪、关系、决策、记忆有强烈求知欲，但市面内容要么过于学术（教材），要么营销化（心灵鸡汤/伪科学/智商税）。
- 缺口：缺少一个**中立、结构化、可交互、中英双语、免费无广告**的心理学通识科普站。
- 系列一致性：`wanwu-hub` 入口站已将本作列为「万物线」成员（副标题「读懂人心」，按「核心流派—认知偏差—著名实验—心理学家词典—生活应用」组织）。

### 1.2 产品目标
1. 用**通俗语言 + 结构化数据 + 可视化**讲清心理学核心概念、经典实验与运行逻辑。
2. 覆盖「概念 → 词典 → 思想家 → 流派 → 认知偏差 → 实验 → 发展史 → 生活应用 → 误区 → 测验 → 跨文化对比」全谱系。
3. 让用户在 3 分钟内能看懂任意一个概念，并能顺藤摸瓜建立体系。
4. 复用经济/历史站工程范式，**低成本、可维护、可无限扩展**。

### 1.3 非目标（Out of scope）
- ❌ 不提供**心理诊断 / 治疗建议 / 用药指导**（合规与中立性红线）。
- ❌ 不做在线测评、问诊、咨询预约或任何医疗相关服务。
- ❌ 不做用户系统 / UGC 社区 / 评论（保持纯静态）。
- ❌ 不做深度学术论文级内容（保持通识与入门定位）。
- ❌ 不做实时数据/行情类可视化（心理学无宏观经济指标，仅做示意性量表/幸福感数据）。

---

## 2. 目标用户

| 用户 | 痛点 | 我们提供 |
| --- | --- | --- |
| 学生 / 心理学入门者 | 教材枯燥、概念抽象 | 图解 + 生活例子 + 术语词典 |
| 职场 / 泛自我成长人群 | 拖延、决策、关系看不懂 | 概念卡 + 实验 + 生活应用 |
| 爱好者 / 好奇者 | 碎片化、不成体系 | 模块化导航 + 体系化脉络 |
| 教师 / 内容创作者 | 缺中英双语、可引用素材 | 结构化 JSON + llms 全文（可被 AI 引用） |

---

## 3. 设计原则（沿用姊妹项目，保持一致）

1. **零依赖**：不引入前端框架，Vite + TypeScript 仅作打包/代码分割；运行时为原生 DOM + 字符串模板渲染。
2. **双语（中/英）**：所有文案成对（`zh` / `en`），`*En` 字段回退中文。
3. **数据驱动**：内容全部落在 `public/data/*.json`，前端只负责渲染。
4. **脚本生成派生文件**：搜索索引、llms 全文、sitemap 等由 `scripts/*.mjs` 生成。
5. **响应式 + 明暗主题**：断点式列数、`min()` 防横向溢出；首屏防主题闪烁。
6. **中立与可信**：概念表述客观，涉及争议（如某些实验的伦理、可重复性危机）给出多方观点并标注来源与年份。
7. **GEO/AI 友好**：产出 `llms.txt` / `llms-full.txt` / `sitemap.xml` / JSON-LD，便于 AI 搜索引擎引用。
8. **可访问性**：语义化标签、键盘可达、对比度达标、`prefers-reduced-motion`。
9. **安全**：所有外部数据经 `esc()` 转义后再进 `innerHTML`，杜绝 XSS。

---

## 4. 信息架构（模块清单）

> 采用顶部「模块导航 + 首页入口卡」两级结构，与姊妹项目一致。优先级：P0 = MVP 必做，P1 = 第二批，P2 = 后续。

| # | 模块 | 英文 | 说明 | 优先级 |
| --- | --- | --- | --- | --- |
| 0 | 概览 | Overview | 首页入口 + 关键指标（概念/术语/心理学家/实验数） | P0 |
| 1 | 核心概念 | Core Concepts | 心智、意识、记忆、情绪、动机、人格、归因、依恋… | P0 |
| 2 | 名词词典 | Glossary | 认知偏差/防御机制/条件反射/图式/依恋…定义+大白话+英 | P0 |
| 3 | 心理学家 | Psychologists | 弗洛伊德、皮亚杰、斯金纳、班杜拉、卡尼曼、马斯洛… | P0 |
| 4 | 心理学流派 | Schools | 精神分析、行为主义、认知、人本、进化、神经、积极 | P0 |
| 5 | 认知偏差 | Cognitive Biases | 确认偏误、锚定、可得性、损失厌恶、从众、幸存者偏差 | P1 |
| 6 | 著名实验 | Experiments | 斯坦福监狱、米尔格拉姆、棉花糖、小阿尔伯特、阿斯奇从众 | P1 |
| 7 | 发展时间轴 | Timeline | 从冯特 1879 到当代，心理学史关键节点 | P1 |
| 8 | 生活中的心理学 | Everyday | 拖延、决策、亲密关系、职场、育儿、社交媒体 | P1 |
| 9 | 常见误区 | Myths | 「左脑人/右脑人」「血型性格」「多元智能测试」辟谣 | P2 |
| 10 | 小测验 | Quiz | 概念自测（选择题，纯前端） | P2 |
| 11 | 中西对比 | Compare | 自我建构、情绪表达、育儿、从众的跨文化差异 | P2 |
| 12 | 心理测量（可选） | Measures | 常见量表/幸福感数据的纯 SVG 示意（替代「数据可视化」） | P2 |

---

## 5. 内容规范

### 5.1 心理学家条目字段（示例 `data/psychologists.json`）
```json
{
  "id": "kahneman",
  "name": "丹尼尔·卡尼曼",
  "nameEn": "Daniel Kahneman",
  "era": "1934–2024",
  "field": "认知心理学 / 行为经济学",
  "fieldEn": "Cognitive Psychology / Behavioral Economics",
  "oneLiner": "用「系统1/系统2」解释人类如何在直觉与理性间切换。",
  "oneLinerEn": "Explained how humans switch between intuitive and rational thinking via System 1 / System 2.",
  "detail": "……（3–6 句，含主要贡献与影响）",
  "detailEn": "……",
  "keyIdeas": ["系统1/系统2", "前景理论", "启发式"],
  "keyIdeasEn": ["System 1/2", "Prospect Theory", "Heuristics"],
  "works": ["思考，快与慢"],
  "worksEn": ["Thinking, Fast and Slow"],
  "related": ["tversky", "anchoring", "loss-aversion"],
  "tags": ["认知", "决策"],
  "sources": [{ "label": "《思考，快与慢》", "year": 2011 }]
}
```

### 5.2 其他模块字段要点
- **核心概念 / 名词词典**：`term` + `termEn` + `plain`（大白话）+ `plainEn` + `related` + `sources`；概念卡含「一句话 + 详解 + 生活例子」。
- **心理学流派**：`core`（核心主张）+ `founders`（代表人物）+ `keyIdeas` + `strengths`（说服力所在）+ `criticisms`（主要批评）。
- **认知偏差**：`manifest`（表现）+ `why`（为什么会这样）+ `avoid`（如何减少影响）+ `example`。
- **著名实验**：`hypothesis`（假设）+ `procedure`（过程）+ `finding`（结论）+ `ethics`（伦理争议，如实标注如斯坦福监狱/小阿尔伯特的争议）。
- **发展时间轴**：`year` + `title` + `what`（发生了什么）+ `why`（为什么重要）+ `impact`（影响了什么）。
- **生活中的心理学**：`scenario`（场景）+ `explanation`（心理学解释）+ `tip`（可操作建议，非医疗建议）。

### 5.3 通用规则
- **难度分级** `level: 1|2|3`（入门/进阶/拓展），前端可筛选。
- **每个概念/偏差/实验必有**：一句话定义 + 通俗解释 + 生活例子/场景 + 相关条目。
- **术语一致**：维护 `data/glossary.json` 作为唯一术语源，其他模块引用其 `id`。
- **数据标注来源**：涉及统计数字或具体结论必须写 `sources`（机构/作者 + 年份，尽量带 `url`）。
- **中立性**：争议话题（实验可重复性、流派优劣、文化差异）并列多方观点，不站队、不诊断、不治疗。
- **双语字段**：一律提供 `*En`，缺失时前端回退中文（`L(o,k)`）。
- **合规**：凡涉及「建议」，限定为「日常可操作的小技巧」，明确「不构成专业心理/医疗建议」；页脚与误区模块显著提示。

---

## 6. 技术架构

> 完全复刻 `econ-everything` 范式，最大化复用经验与脚本。

```
mind-everything/
├─ index.html              # 单页应用：i18n + 模块切换 + 渲染
├─ vite.config.ts          # base: build 时 '/mind-everything/'
├─ tsconfig.json
├─ favicon.svg
├─ og-cover.svg
├─ robots.txt / sitemap.xml / llms.txt / llms-en.txt / llms-full.txt / llms-full-en.txt
├─ public/data/
│  ├─ overview.json        # 首页/概览（KPI + 入口 + 参考文献）
│  ├─ concepts.json        # 核心概念
│  ├─ glossary.json        # 名词词典
│  ├─ psychologists.json   # 心理学家
│  ├─ schools.json         # 心理学流派
│  ├─ biases.json          # 认知偏差
│  ├─ experiments.json     # 著名实验
│  ├─ timeline.json        # 发展时间轴
│  ├─ everyday.json        # 生活中的心理学
│  ├─ myths.json           # 常见误区
│  ├─ quiz.json            # 小测验
│  ├─ compare.json         # 中西对比
│  ├─ measures.json        # 心理测量（P2 可选）
│  └─ search.json          # 由脚本生成
├─ scripts/
│  ├─ build-all.mjs        # 串联下列生成
│  ├─ build-overview.mjs   # 汇总 KPI / 入口 / 参考文献
│  ├─ build-search.mjs     # 生成搜索索引
│  ├─ build-geo.mjs        # 生成 llms(.en)/robots/sitemap
│  ├─ build-llms-full.mjs  # 生成 llms-full(.en)
│  └─ check-i18n.mjs       # 双语字段完整性校验
└─ src/
   ├─ main.ts
   ├─ core/                # i18n / theme / data / dom / app / detail / search / related / ui / types
   ├─ modules/             # 12 个内容模块 + shared.ts + types.ts
   └─ styles/              # tokens.css / base.css / components.css / index.css
```

- **运行方式**：`npm run dev`（本地 `/`）调试；`npm run build` 输出 `dist/`（base=`/mind-everything/`）用于 GitHub Pages 部署。
- **模块机制**：`data-go="m-xxx"` 按钮 + `.module.active` 切换，与姊妹项目一致。
- **响应式**：卡片网格用 `repeat(auto-fill, minmax(min(Xpx,100%), 1fr))`，断点 900 / 560px。
- **i18n**：`I18N.zh / I18N.en`，`t(key)` / `L(obj,key)`；语言存 `localStorage`，支持 `?lang=en`。

---

## 7. 关键功能（MVP 范围）

| 功能 | 说明 | 优先级 |
| --- | --- | --- |
| 模块导航 + 首页入口 | 顶栏 tab + 首页卡片进入 12 个模块 | P0 |
| 全站搜索 | 概念 / 术语 / 心理学家 / 实验 / 流派，实时联想，点击跳转 | P0 |
| 概念卡与词典 | 卡片列表 + 难度/分类筛选 | P0 |
| 明暗主题 | 跟随系统 + 手动切换，首屏防闪烁 | P0 |
| 中英切换 | 全站即时切换，`?lang=en` 深链 | P0 |
| 相关条目跳转 | 跨模块互链，形成知识网 | P1 |
| 著名实验 / 时间轴 | 时间线/列表组件，点击看详情与伦理争议 | P1 |
| 小测验 | 选择题自测 + 解析 | P2 |
| 心理测量 SVG | 示意性量表/幸福感数据（P2 可选） | P2 |

---

## 8. UI/UX 要求

- **视觉**：延续系列卡片式、柔和阴影；品牌色另立「心理/心智」配色（建议：靛蓝 `#4F46E5` + 暖橙 `#E0A458`，与经济的青/金、历史的红/金区分）。
- **布局**：模块标题居中；内容卡片自适应多列；移动端单列；**移动端不得出现横向滚动条**。
- **交互**：卡片 hover 上浮、点击跳转、键盘可达（Enter/Space）、`aria` 标注。
- **反馈入口**：hero 区提供 GitHub Issues + 邮箱（`✉️ 邮件反馈`，`mailto:` 带预设主题/正文）。
- **性能**：首屏 < 100KB（不含图片）；数据按模块懒加载。
- **合规提示**：页脚与「常见误区」显著标注「本站为通识科普，不构成任何专业心理/医疗建议」。

---

## 9. SEO / GEO 要求

- 每页 `<title>` / `description` / `keywords` / OG / Twitter / `hreflang`（zh / en / x-default）/ canonical。
- JSON-LD：`ItemList`（概念/心理学家）+ `WebSite`。
- 产出 `llms.txt` / `llms-full.txt`（中英）+ `sitemap.xml`（含各 `data/*.json`）+ `robots.txt`。
- 统计：可接入百度统计 / GA（占位，尊重隐私，不采集敏感信息）。

---

## 10. 成功指标（KPI）

| 指标 | 目标（上线 3 个月） |
| --- | --- |
| 概念/术语覆盖 | ≥ 100 个概念、≥ 150 条术语 |
| 人物/流派覆盖 | ≥ 40 位心理学家、≥ 12 个流派 |
| 偏差/实验覆盖 | ≥ 40 条认知偏差、≥ 25 个著名实验 |
| 模块完整度 | P0 全部 + P1 ≥ 80% |
| 双语完整度 | `check-i18n` 零缺失 |
| 移动端体验 | 无横向滚动、Lighthouse 性能 ≥ 90 |
| 自然流量 | 稳定增长；被 AI 搜索引用（`llms-full` 抓取） |

---

## 11. 里程碑（Roadmap）

| 阶段 | 内容 | 交付 |
| --- | --- | --- |
| **M0 脚手架** | 目录结构、`index.html/styles` 骨架、i18n、模块切换、明暗主题、构建脚本 | 可运行空壳 |
| **M1 MVP** | P0 模块：核心概念、名词词典、心理学家、心理学流派 + 搜索 + GEO 文件 | 可发布 v0.1 |
| **M2 体系化** | P1：认知偏差、著名实验、发展时间轴、生活中的心理学 | v0.5 |
| **M3 增强** | P2：常见误区、小测验、中西对比、心理测量（可选） | v1.0 |
| **M4 运营** | 统计接入、内容扩充、被 AI 引用优化、反馈闭环 | 持续 |

---

## 12. 风险与对策

| 风险 | 影响 | 对策 |
| --- | --- | --- |
| 内容准确性 / 合规 | 误导、医疗合规风险 | 中立表述、标注来源、不诊断不治疗、避免预测 |
| 实验可重复性争议 | 可信度受损 | 如实标注伦理争议与可重复性危机（如 Replication Crisis） |
| 概念过多导致散乱 | 用户迷路 | 难度分级 + 分类筛选 + 相关互链 + 搜索 |
| 双语工作量大 | 维护成本 | 脚本校验缺失（`check-i18n`）、`*En` 回退 |
| 与姊妹站重复造轮子 | 效率低 | 直接复用架构/脚本/CSS 变量与组件 |

---

## 13. 开放问题（待定）

1. 视觉主题色：沿用系列红/金，还是另立「心理/心智」配色（靛蓝 + 暖橙）？
2. 是否纳入「心理测量」模块（P2 可选），用纯 SVG 示意量表/幸福感数据？
3. 测验是否要做「闯关/进度」？
4. 英文站是同一页切换，还是独立 `en/` 目录（当前倾向同页切换）？
5. 是否补充「应用分支」子模块（如临床/组织/发展/社会心理学）作为流派之外的第二维度？

---

## 14. 命名与品牌

- **主名**：万物心理学
- **英文副题**：Mind of Everything
- **展示句式**：`万物心理学 · Mind of Everything`（延续「万物经济学 · Economics of Everything」）
- **域名 / 仓库**：`mind-everything`
- **Slogan 备选**：
  - 用心理学，读懂人心。
  - 万物皆可心理学。
  - 把人心运行的规律，讲成大白话。

---

> 下一步：确认第 13 节开放问题后，即可进入 **M0 脚手架**，直接复用「万物经济学」的工程范式搭出可运行空壳。
